-> Mongo DB is an open souce NOsql database management program 
-> Nosql is used as a alternative to traditional relational database
-> Nosql databases are quite useful for working with large sets of distributed data.

-> Mongodb is used for merged an document-oriented information, store and retrive information


##### Benifit of mongoDB #############

-> MOngoDB is designed to make data easy to access, rerely to require joins or transactions

# Why we use MOngoDB instead of MySQl
-> MOngoDB is faster then MySQL due to its ability to handle large amount of unstructured data when it comes in speed

-> it uses slave replication, master replication to process vast amount of unstructured data and offers the freedom to use multiple data types that are better than the rigidity of MySQl

-> schema are used to execute the 